/**
 * Drizzle table definitions.
 *
 * TODO(phase-0): tables land here per `schema.md` (feature "Database schema and
 * migrations for all domain objects"). Until then the schema is deliberately
 * empty: `drizzle-kit generate` reports no changes and no migrations exist.
 */
export {}
